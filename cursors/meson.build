cursor_dir = 'Yaru'
theme_name = meson.project_name()
theme_install_dir = join_paths(get_option('prefix'), 'share/icons', theme_name)

install_subdir(cursor_dir,
  install_dir: theme_install_dir,
  strip_directory: true,
)
